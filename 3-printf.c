#include "main.h"

/**
 * handle_unsigned_int - prints unsigned interger
 * @list: variable argument
 * Return: Always 0 (succes)
*/
int handle_unsigned_int(va_list list)
{
	return (0);
}

/**
 * handle_unsigned_octal - print unsigned octal
 * @list: variable argument
 * Return: Always 0 (Success)
*/
int handle_unsigned_octal(va_list list)
{
	return (0);
}

/**
 * handle_hexadecimal - converts integer to hexadecimal
 * @list: variable argument
 * Return: Always 0 (Success)
*/
int handle_hexadecimal(va_list list)
{
	return (0);
}
